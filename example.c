#include <stdio.h>
#include <stdlib.h>

void main(int argc, char *argv[])
{
  int p_arg = atoi(argv[1]);
  int s_arg = atoi(argv[2]);

  printf("La suma de los dos argumentos es : %d \n", p_arg + s_arg);
  printf("Este codigo se ejecuto en c XD\n");
}
